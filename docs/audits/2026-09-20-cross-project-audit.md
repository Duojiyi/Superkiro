# 全项目交叉审计补充（2026-09-20）

## 范围与方法

基于 02c2fcb 继续检查桌面 React → Tauri → patch-engine、portal/admin → gateway → billing 以及生产部署配置。桌面底层、云端计费、前端契约由独立代理分别检查，主流程整合并交叉复核。生产仅只读健康、端口、权限、备份与响应头，不执行真实卡密扣费、解绑或 IDE 修改。自动化 fixture 通过不等同于真实客户端或生产验收。

## 本轮确认并修复

| 范围 | 发现 | 修复及证据 |
| --- | --- | --- |
| 计费幂等 | janitor 七天后清理 Settled reservation，导致已结算 invocation 可再次预留并扣费 | 保留已结算 ID；旧快照导入时从仍存在的 Usage 明细一次回填，不增加每次请求的历史扫描；新增清理、归档、重启、旧快照回归 |
| 本地文件保护 | settings.json 顶层数组/null/标量被当成空对象，接管/还原可覆盖或删除内容 | 统一拒绝非对象配置，验证原始字节不变 |
| 解绑状态 | 云端解绑成功、本地恢复失败后，会话仍显示 authenticated | 先持久化认证失效，再恢复；保留恢复材料，fixture 模拟损坏快照并重开会话 |
| 桌面操作锁 | 打开 Kiro 未复用变更操作协调，超时可能再次提交 | launch 走统一 mutate/operation 协调；拒绝缺少 success:true 的伪成功 |
| 桌面用量 | 本地还原后仍显示不再更新的今日用量 | 清空当日结算用量；余额保留时间戳并标注非实时 |
| 换绑契约 | 冻结卡密解绑后被告知可登录；冷却和次数耗尽均为通用重试 | 成功仅确认解绑；稳定白名单错误码与校验过的秒数，同步到桌面 AuthClient 与中文安全提示；文档同步卡密状态、余额、有效期限制；其他错误不回传内部设备/卡密标识 |
| 指标权限 | 生产 /metrics 可匿名读取业务请求与计费汇总 | 纳入管理员认证，验证匿名和客户端 JWT 拒绝、管理员 key/cookie 允许 |
| 生产字体 | CSP 未允许 data 字体，官网无法使用设计字体 | 仅加入 font-src 'self' data:；部署保留现有 Caddy 路由，只迁移该策略；浏览器以实际生产配置 CSP 验证字体加载 |
| 登录记录 | 防暴力破解失败来源永久保留 | 新失败时清理过期记录，保留仍有效封禁；拒绝采用容量满时阻断全部新来源的方案 |
| CI | 管理后台视觉测试仍使用旧定价标签 | 更新为当前可访问名称，真实浏览器复测通过 |

新增计费并发用例还验证 16 线程日/月预算预留只能有一个成功，以及撤销后阻止新请求但仍结算已消费的在途请求。网关流式断连、部分 EOF、重复 Done、结算 I/O 故障及冷启动恢复已有测试复核。

## 生产只读结果

- https://kiro.rent/healthz 返回 200；管理 stats 匿名 401。
- Gateway/Caddy 正常，应用仅通过 80/443 对外；配置目录 700、环境文件 600、数据目录 750。
- 备份 timer 激活，最近 service 成功；磁盘约 21 GB 可用。这不证明灾难恢复已演练。
- 确认现网 metrics 匿名 200 和字体 CSP 缺失；工作区修复未部署前，两项仍存在。
- 本轮不提升已构建但不含本轮修复的服务器候选，不将旧原生构建标记为本轮修复版本。

## 剩余边界

- 升级前已丢弃去重记录、且明细也已归档的历史 ID，不会自动从归档重建。本轮仅补齐仍在快照明细中的 ID。生产网关生成请求 ID，不提供可任意重放外部请求 ID 的承诺。
- Settled 去重记录保留会增长快照；当前全局写锁与快照持久化不等同于高吞吐数据库。需容量基准再设计紧凑 tombstone/存储迁移。
- 防暴力破解按活跃时间窗保留来源，未做分布式大规模攻击压测；清理为 O(n)，不声称提供无限容量保护。
- 未完成真实 Windows/macOS IDE 接管、升级并发、断电、系统钥匙串和应用级 Quit 的全部验收；现有忽略项不算通过。未知扩展哈希仍应保留备份并拒绝覆盖，不能绕过保护来声称恢复成功。
- 云端解绑与本地落盘不是跨网络事务；解绑成功后磁盘写入失败/进程崩溃仍需重新读取云端授权与本地恢复材料。接管期间用户另行官方登录的凭据归属未做专门原生验收。
- 安装包仍为未签名/未公证 preview；手动更新，不是自动更新。异地备份、恢复演练和持续生产负载验证尚未完成。
- 没有实施真实付费模型请求，不能用本地 fixture 证明所有上游模型在生产可用。

## 验证记录

- 桌面前端：116 项通过，TypeScript/Vite 构建通过；30 组布局状态通过，浏览器状态/对比度检查通过。布局首轮有一次超时，完整复测通过，未放宽断言。
- 原生宿主 backend：34 通过、1 忽略；只测试后端 fixture，不等于原生窗口/真实 IDE 验收。
- 管理后台：构建、单元/契约、匿名权限、公开登录、九页真实交互、可用性、批量卡密、迭代浏览器套件全部通过。
- 官网：18 项交互测试、5 项 Pencil/字体 CSP 检查通过；代理补充的 device 文档断言单独通过。
- 桥接/运行时/部署 CSP：28 项通过；安装包发布 9 项、来源校验 5 项通过。
- b0167a5 本地 Rust workspace 全量回归 523 通过、1 忽略，退出码 0；严格 Clippy 已通过。GitHub Windows Rust、前端、安全扫描均通过，Linux Rust 出现操作锁竞态，见下文。
- 本机备份脚本 46 项：38 通过、2 跳过、6 项 Git Bash 子进程 30 秒超时，不能记为全通过；同提交 GitHub Ubuntu/Windows 备份步骤均通过。
- git diff --check 通过；本轮修改 diff 的 Gitleaks 脱敏扫描通过。

详细本地日志位于 .acceptance/review-*。测试期间曾因旧测试进程占用 Windows 可执行文件出现 LNK1104，等待旧进程结束后重跑；该失败不计为通过。首轮与最终代码不同或被中止的执行不计入最终通过数。

## CI 追加发现：Unix 操作锁释放

- b0167a5 Linux CI 的认证失效后再次读取用量测试一次返回会话锁被占用。原实现只关闭文件；Unix fork/dup 共享 open file description，其他线程启动子进程时继承的描述符可能使锁延迟释放。
- OperationLock 在 Drop 中显式 unlock，仍保留锁文件 inode，不删除文件、不放宽锁冲突断言。新增 Unix dup 回归确定性覆盖句柄仍存活时释放所有权、以及旧句柄关闭不影响新持有者。追加修复本地 patch-engine lib 60 通过、1 忽略，严格 Clippy 通过。新增 Unix 用例仍待 Linux CI；独立复核认可锁语义，但尚未直接复现原 CI 的 fork 调度，不能断言已证实唯一根因。
- b0167a5 的 Windows、macOS x64、macOS arm64 安装包构建均成功；追加锁修复后必须重建，不能混用来源提交。生产未更新。
