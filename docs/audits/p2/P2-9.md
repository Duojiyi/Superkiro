# 双盲审计报告：P2-9 可观测与财务对账（Observability & Financial Reconciliation）

**日期**：2026-09-10  
**任务**：P2-9 可观测：request_trace / 按日聚合 / 成本 vs 收入毛利看板 / 模型成本排行 / provider 健康大盘 / 用量异常告警（速率突增→告警+自动限速） / 降级公告下发 / 导出 CSV-JSON / 数据保留策略（Spec §14.4, §14.9, §14.10）  
**关联代码**：
- `crates/billing/src/observability.rs`
- `crates/billing/src/engine.rs`
- `crates/billing/src/schema.sql`
- `crates/billing/src/lib.rs`
- `crates/billing/tests/observability_test.rs`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照系统架构规范 Spec §14.4（运营指标与可观测性）、Spec §14.9（运营层与系统公告）与 Spec §14.10（财务核算与对账）逐项核验：

| 规范章节 | 规范条款与要求 | 代码落地位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **Spec §14.4** | **全链路调用追踪 (Request Trace)**：记录卡密、请求时间、耗时、TTFT (Time to First Token)、tok/s、状态、错误类、消耗 token 与重试链路 (`attempt_chain`)。 | `crates/billing/src/observability.rs::RequestTrace`, `engine.rs::record_trace` | **PASS** |
| **Spec §14.4** | **按日用量多维聚合**：支持按日分组聚合请求量、各类型 token（输入/输出/缓存写入/缓存读取）、积分收入与供应商微元成本。 | `crates/billing/src/engine.rs::get_daily_summary` | **PASS** |
| **Spec §14.10** | **毛利与财务看板 (Margin Dashboard)**：基于全局 `settings` 汇率与面值计算毛利额与毛利率（`gross_margin_percentage`），支持总收入、总成本核算。 | `crates/billing/src/observability.rs::compute_margin_dashboard` | **PASS** |
| **Spec §14.10** | **模型成本与利润排行榜**：按模型统计请求次数、吞吐量、总成本微元、积分收益与各自毛利率，按成本从大到小降序排列。 | `crates/billing/src/observability.rs::compute_model_cost_rankings` | **PASS** |
| **Spec §14.4** | **Provider 健康大盘指标**：聚合供应商调用次数、成功率（`success_rate`）、平均首字延迟（TTFT）、平均吞吐量（tok/s）与错误分布。 | `crates/billing/src/observability.rs::compute_provider_health` | **PASS** |
| **Spec §14.4 & §14.9** | **用量突增异常检测与自动防护**：滑动窗口突发消耗评估，超过设定阈值自动产生告警并支持触发自动冻结卡密（`AnomalyAction::AutoFrozen`）。 | `crates/billing/src/engine.rs::evaluate_card_spike` | **PASS** |
| **Spec §14.9** | **运营与服务降级公告**：发布各级别公告（Info/Warning/Critical/Maintenance），按目标分组过滤，过期自动下线。 | `crates/billing/src/observability.rs::Announcement`, `engine.rs::list_active_announcements` | **PASS** |
| **Spec §14.10** | **对账数据导出 (CSV / JSON)**：提供符合审计要求的双格式对账导出（时间、卡号脱敏、模型、Provider、Token 明细、积分与供应商成本）。 | `crates/billing/src/observability.rs::export_reconciliation_csv`, `export_reconciliation_json` | **PASS** |
| **Spec §14.4** | **数据保留生命周期策略**：支持按天/秒就地清理过期调用轨迹（Pruning），避免数据库与内存无界膨胀。 | `crates/billing/src/observability.rs::prune_traces_in_place`, `engine.rs::prune_traces` | **PASS** |

**审计 A 结论**：全项符合 Spec §14.4、§14.9 与 §14.10 规范，无偏差。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

在 `crates/billing/tests/observability_test.rs` 中实施对抗演练（覆盖 9 个独立用例）：

### 对抗场景 1：全链路 Request Trace 记录与过滤查询
- **测试**：`test_record_and_list_request_traces`
- **验证**：记录成功与超时失败轨迹，包含 TTFT、tok/s 以及包含故障转移的 `attempt_chain`。
- **实测**：根据卡密查询轨迹，精确提取重试链路；包含 2 次上游尝试（首次超时失败，二次切换成功）。
- **实测结果**：**PASS**。

### 对抗场景 2：跨日用量聚合与跨天分组正确性
- **测试**：`test_daily_usage_aggregation_and_multi_day_reports`
- **验证**：跨越自然日（86,400s 边界）的连续请求记录。
- **实测**：两日分别准确聚合 `requests_count`、输入/输出 token、微积分消耗与供应商成本，自然日索引正确。
- **实测结果**：**PASS**。

### 对抗场景 3：成本 vs 收入综合毛利看板测算
- **测试**：`test_cost_vs_revenue_gross_margin_dashboard`
- **验证**：设置积分面值 0.0001 元，输入成本 1,000,000 微元（1元），消耗积分 30,000,000（3元）。
- **实测**：精确算出毛利 2,000,000 微元，毛利率 66.67%，成本收入比完全闭环。
- **实测结果**：**PASS**。

### 对抗场景 4：多模型成本贡献度排序
- **测试**：`test_model_cost_rankings`
- **验证**：同时结算 `claude-sonnet-4.5` 与 `gpt-4o-mini`，成本分别为 1.5 元与 0.1 元。
- **实测**：自动按供应商成本降序排列，高成本模型排在首位，吞吐量与利润率精确计算。
- **实测结果**：**PASS**。

### 对抗场景 5：Provider 健康大盘首字延迟与吞吐统计
- **测试**：`test_provider_health_summary_metrics`
- **验证**：上游混合返回成功（TTFT 200ms/400ms，速率 50/30 tok/s）与失败错误。
- **实测**：`success_rate` 正确计算为 66.67%，平均 TTFT 300ms，平均速率 40.0 tok/s，错误类型准确归类。
- **实测结果**：**PASS**。

### 对抗场景 6：异常突发消费告警与自动冻结防护
- **测试**：`test_usage_anomaly_detection_and_auto_freeze`
- **验证**：在 300 秒滑动窗口内设定 4,000,000 阈值，注入 4,500,000 突增消费并配置 `AnomalyAction::AutoFrozen`。
- **实测**：精准触发 `AnomalyAlert`，并且卡密状态立即被自动变更为 `CardStatus::Frozen`，拦截后续调用。
- **实测结果**：**PASS**。

### 对抗场景 7：系统公告目标分组过滤与 TTL 过期测试
- **测试**：`test_announcements_downlink_and_expiry`
- **验证**：下发全局维护公告（1小时后过期）与分组专有提示公告（已过期）。
- **实测**：活跃公告查询时已过期公告被自动剔除；根据 `group_id` 进行准确定向匹配。
- **实测结果**：**PASS**。

### 对抗场景 8：财务对账 CSV 与 JSON 双格式无损导出
- **测试**：`test_financial_reconciliation_export_csv_and_json`
- **验证**：导出包含时间戳、脱敏卡号、模型、token 与积分成本的账本。
- **实测**：CSV 包含完整标题行且字符精确无歧义；JSON 符合规范且可无损反序列化解析。
- **实测结果**：**PASS**。

### 对抗场景 9：数据保留生命周期就地安全剪裁
- **测试**：`test_data_retention_pruning_policy`
- **验证**：对 30 天保留期的轨迹表进行剪裁，传入过期截断时间戳。
- **实测**：过期轨迹被就地回收，保留期内轨迹完整无损，无内存泄漏与数据竞争。
- **实测结果**：**PASS**。

---

## 自动化测试与质量指标

- **新增测试用例**：`crates/billing/tests/observability_test.rs`（9 个测试全绿）
- **全工作区测试覆盖**：`cargo test --workspace` -> **151 passed; 0 failed**
- **代码静态分析**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**
- **代码风格**：遵循 Ponytail 极简原则，原生整数运算，零冗余抽象。

---

## 汇合结论

- **审计 A 结论**：PASS（全部规范项完全落实）  
- **审计 B 结论**：PASS（全部边界对抗与异常场景验证通过）  
- **最终审计结论**：**PASS**，任务 P2-9 正式完成。
