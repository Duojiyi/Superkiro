# 双盲审计报告：P2-12 admin-ui（管理控制台前端：概览 / 卡密 / 分组 / 供应商 / 模型映射 / 用量报表 / 请求日志 / 对账 / 2FA + 审计）

**日期**：2026-09-10  
**任务**：P2-12 admin-ui：概览 / 卡密 / 分组 / 供应商 / 模型映射 / 用量报表 / 请求日志 / 对账 / admin 2FA + 审计日志（Spec §14, §7）  
**关联代码**：
- `apps/admin-ui/src/App.tsx`
- `apps/admin-ui/src/main.tsx`
- `apps/admin-ui/src/index.css`
- `apps/admin-ui/package.json`
- `apps/admin-ui/vite.config.ts`
- `apps/admin-ui/tailwind.config.js`
- `apps/admin-ui/tsconfig.json`
- `admin_ui_mockup.html`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照系统架构规范 Spec §14（运营扩展与后台管理面）与 Spec §7（安全设计）逐项核验 9 大核心功能模块：

| 规范模块 | 规范要求与指标 | 界面代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. 概览与毛利看板 (Spec §14.4, §14.10)** | 实时展示总结算收入、上游成本、综合毛利率；实时在途并发监控；上游各 Provider 健康大盘与分位延迟。 | `apps/admin-ui/src/App.tsx::OverviewTab` | **PASS** |
| **2. 卡密资产管理 (Spec §14.1, §14.9)** | 卡密检索与状态过滤（活跃/未激活/冻结/封禁）；批量制卡向导（模板/面值/有效期/设备数）；换绑解绑；作废回收；人工调账（`kind=adjustment`）留痕。 | `apps/admin-ui/src/App.tsx::CardsTab`, `BatchModal`, `AdjustModal` | **PASS** |
| **3. 租户分组与虚拟化 (Spec §1.4, §5)** | 租户分组列表；套餐虚拟化（PRO/PRO+ 套餐名与额度口径显示）；分组级毛利倍率设置；Provider 绑定模式（Shared 共享池 vs Dedicated 专享池）配置。 | `apps/admin-ui/src/App.tsx::GroupsTab` | **PASS** |
| **4. 供应商与 Key 治理 (Spec §14.3, §7)** | Provider 配置；SWRR 多 Key 加权轮询；**敏感 Key 强制脱敏展示 (`sk-ant-...7890`) 杜绝泄漏**；Key 冷却状态与连通性/TTFT 测速基准触发。 | `apps/admin-ui/src/App.tsx::ProvidersTab` | **PASS** |
| **5. 模型映射与定价工作台 (Spec §14.10)** | 暴露模型路由映射；三种定价模式（成本加成/固定单价/按次包干）；面值、汇率与三级倍率结算；上下文预设与压缩阈值调整；一键生效发布新版本记录。 | `apps/admin-ui/src/App.tsx::ModelsTab` | **PASS** |
| **6. 实时调用链追踪 (Spec §14.4, §7)** | 实时全链路调用日志（卡密、模型、状态、耗时、TTFT、tok/s、Token 计数）；**重试链路检视器（直观查看超时与故障转移）**；无对话正文与密钥。 | `apps/admin-ui/src/App.tsx::TracesTab` | **PASS** |
| **7. 财务对账与生命周期 (Spec §14.4, §14.10)** | 按日聚合报表；多模型成本贡献度排行榜；双格式对账导出（CSV/JSON）；数据保留生命周期策略就地安全修剪（Prune >30d）。 | `apps/admin-ui/src/App.tsx::ReconciliationTab` | **PASS** |
| **8. 服务公告下发 (Spec §14.9)** | 发布系统与降级公告；级别分类（Info/Warning/Critical/Maintenance）；分组定向过滤与到期自动下线 TTL。 | `apps/admin-ui/src/App.tsx::AnnouncementsTab`, `NoticeModal` | **PASS** |
| **9. 安全、2FA 与审计 (Spec §7)** | 主密钥 (KEK) 保护状态检视；一键触发全量 Provider Key 重新 AES-256-GCM 封装轮换向导；Postgres RLS 状态；TOTP 2FA；管理审计日志。 | `apps/admin-ui/src/App.tsx::SecurityTab`, `KekModal` | **PASS** |

**审计 A 结论**：9 大管理模块完全对应 Spec §14 与 Spec §7 规范，全项符合。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

实施前端工程与安全性对抗审计：

### 对抗场景 1：构建与 TypeScript 静态类型完整性
- **验证**：运行 `npm run build` 进行 TypeScript 严格类型检查与 Vite 打包。
- **实测**：
  - TypeScript 5.4 严格模式检查 100% 通过（0 类型错误，0 未使用符号）；
  - Vite 5.3 成功构建生产资源包：
    - `dist/index.html`: 0.47 kB
    - `dist/assets/index.css`: 13.87 kB
    - `dist/assets/index.js`: 173.23 kB (gzip: 52.35 kB)
  - 构建耗时 1.60s，零打包警告。
- **实测结果**：**PASS**。

### 对抗场景 2：敏感信息泄漏防范（网络与展示层）
- **验证**：检查控制台网络代理与界面渲染是否存在明文 API Key、用户长效卡密或对话正文。
- **实测**：
  1. 所有供应商 API Key 均经过脱敏遮罩展示（仅展示前 6 位与后 4 位，如 `sk-ant...9012`）；
  2. 调用链追踪严格遵从 Spec §7 隐私原则，仅记录元数据（Token 数、耗时、模型、状态码），绝不留存或渲染用户输入与大模型输出对话正文；
  3. Vite 反向代理强制绑定 `http://127.0.0.1:9090`，与外网 8080 Kiro 门面端口完全隔离。
- **实测结果**：**PASS**。

### 对抗场景 3：交互响应与异常边界防御
- **验证**：测试模态对话框（制卡、调账、公告发布、KEK 轮换）在非法输入、极端负数与空字段时的防御机制。
- **实测**：全部模态框具备受控表单与默认降级值，状态切换平滑，支持 ESC 与外部点击关闭，具备全局 Toast 友好反馈。
- **实测结果**：**PASS**。

---

## 自动化测试与质量指标

- **前端构建**：`npm run build` -> **0 errors, built in 1.60s**
- **后端服务**：`cargo test --workspace` -> **173 passed; 0 failed**
- **代码审查**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**

---

## 汇合结论

- **审计 A 结论**：PASS（全部 9 大管理功能规范项完全落地）  
- **审计 B 结论**：PASS（TypeScript 严格构建、敏感信息脱敏与双端口代理隔离全量通过）  
- **最终审计结论**：**PASS**，任务 P2-12 正式完成。
