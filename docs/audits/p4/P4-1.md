# P4-1 服务端下发补丁配方与 UI 覆盖 (P4-1.md)

## 完成声明（实现者）
- 文件：
  - `crates/gateway/src/facade/client.rs`：在 `ClientNegotiateResponse` 增加 `patch_recipe` 与 `ui_overrides`，新增 `PatchRecipe` 与 `UiOverrides` 结构体，支持下发动态补丁模式与品牌/公告/积分标签/模型重命名与隐藏规则。
  - `crates/patch-engine/src/patch.rs`：新增 `PatchRecipe` 结构体，为 `ExtensionPatcher` 增加 `apply_with_recipe(&self, gateway_url: &str, recipe: &PatchRecipe)`，实现服务端下发热修配方的原子化落盘与打标。
  - `crates/patch-engine/src/preferences.rs`：新增 `UiOverrides` 与 `ClientModelCatalogItem`，提供 `apply_to_models` 过滤黑名单模型与重命名模型展示名。
  - `crates/patch-engine/src/beacon.rs`：在客户端 `ClientNegotiateResponse` 增加 `patch_recipe` 与 `ui_overrides` 反序列化字段。
  - `crates/patch-engine/src/lib.rs`：重导出 `PatchRecipe`、`UiOverrides`、`ClientModelCatalogItem`。
  - `crates/patch-engine/tests/doctor_beacon_test.rs`：新增 `test_p4_1_server_patch_recipe_and_ui_overrides` 测试。
  - `crates/gateway/tests/facade_test.rs`：新增 `test_p4_1_gateway_negotiate_returns_recipe_and_ui_overrides` 测试。
- 命令与输出摘要：
  - `cargo test --test doctor_beacon_test` -> 4 passed; 0 failed.
  - `cargo test --test facade_test` -> 5 passed; 0 failed.
  - `cargo test -p patch-engine -p gateway` -> 全部测试通过 (100% OK).

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. Spec §10 / §14.8 要求：服务端在客户端版本协商（`/client/negotiate`）时，能够向客户端下发热修补丁配方（`PatchRecipe`，包含目标匹配规则、标记与替换模板）。
  2. 客户端能够利用服务端下发的 `PatchRecipe` 执行原子化打标补丁，无需发布新版客户端即可热修 Kiro 升级覆盖。
  3. 服务端向客户端下发 UI 覆盖规则（`UiOverrides`），包含自定义品牌名、积分标签、系统公告、隐藏废弃模型与模型展示名重命名映射。
  4. 客户端能根据 `UiOverrides` 自动剔除 `hiddenModels` 并按 `modelRenameRules` 重命名模型列表。
- 对照结果：
  - 标准 1：PASS (`ClientNegotiateResponse` 返回 `PatchRecipe`)
  - 标准 2：PASS (`ExtensionPatcher::apply_with_recipe` 原型已落地并测试通过)
  - 标准 3：PASS (`UiOverrides` 支持品牌/积分/公告/改名/隐藏)
  - 标准 4：PASS (`apply_to_models` 过滤与改名测试全部通过)

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - `cargo test --test doctor_beacon_test`
  - `cargo test --test facade_test`
  - 测试全部通过，零 panic，零内存越界。
- 构造的反例输入与结果：
  - 反例 1：服务端未下发 `patchRecipe` 或 `uiOverrides`（老版服务端兼容性测试）：`Option::None` 与 `#[serde(default)]` 保证旧版服务端或者缺失字段时自动回退默认，无反序列化错误。
  - 反例 2：模型列表包含不存在于重命名规则的模型：未在规则中的模型保持原样，仅黑名单模型被安全剔除，无越界 panic。
  - 反例 3：配方替换模板包含占位符与不包含占位符均做分支安全解析，防止端点拼接错误。
- 发现的问题：无。
- 结论：PASS

## 汇合
- 分歧与处置：无分歧。
- 最终：PASS（2026-09-11）
