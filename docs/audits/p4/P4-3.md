# P4-3 `/mcp` web search 接自有搜索源 (P4-3.md)

## 完成声明（实现者）
- 文件：
  - `crates/gateway/src/facade/mcp.rs`：新增 `McpHandler`，完整实现 JSON-RPC 2.0 MCP 协议规范。提供 `initialize` 握手、`tools/list` 工具目录声明（`web_search`）与 `tools/call` 执行。支持对接自定义搜索后端（如 SearXNG、自有搜索集群、Tavily/Bing/Google 等）以及内置 DuckDuckGo / 纯文本搜索回退源，精准去除常见前缀 `Perform a web search for the query:` 并将 `SearchResultItem` 格式化为与 Kiro IDE 原生匹配的 JSON 字符串。
  - `crates/gateway/src/facade/mod.rs`：将 `mcp::McpHandler` 注册进网关门面注册表 (`register_virtualized_facades`)。
  - `crates/gateway/tests/mcp_test.rs`：新增完整的集成测试，覆盖 `initialize` 协议版本握手、`tools/list` 列举 `web_search`、自定义搜索后端 WireMock 转发、前缀剥离与查询清理，以及空/不可达网络下的弹性回退和无效方法错误响应。
- 命令与输出摘要：
  - `cargo test --test mcp_test` -> 4 passed; 0 failed.
  - `cargo test --workspace` -> 全部通过 (100% OK, 0 failed).

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. Spec §15.1 / §18.7 要求：网关提供 POST `/mcp` 端点，遵循标准 JSON-RPC 2.0 格式。
  2. 支持 Kiro IDE 发起的 MCP 握手（`initialize`）与工具发现（`tools/list`），输出包含 `web_search` 及其 inputSchema。
  3. 支持工具调用 `tools/call`（name: `web_search`），自动剥离前缀并检索自有搜索源，返回 `content[0].text` 为序列化 JSON 字符串（包含 `results`, `totalResults`, `query`），字段包含 `title`, `url`, `snippet`。
  4. 支持可配置的自有搜索源 URL 与 API Key；在外部源未配置或不可达时安全降级，不阻断、不崩盘。
- 对照结果：
  1. 标准 1：PASS (JSON-RPC 2.0 请求与响应标准实现)
  2. 标准 2：PASS (`initialize` 与 `tools/list` 正常工作)
  3. 标准 3：PASS (`tools/call` 返回规范的 `SearchResponsePayload`)
  4. 标准 4：PASS (可插拔 `SearchSourceConfig` 与弹性回退测试通过)

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - `cargo test --test mcp_test`
  - `cargo test --workspace`
  - 零 panic，零内存越界。
- 构造的反例输入与结果：
  - 反例 1：传入带冗余前缀的 query（`Perform a web search for the query: rust 2026 edition`）：前缀被精准剥离，上游接收纯净 query。
  - 反例 2：调用未定义的 tool（`unknown_tool`）或方法（`unknown_method`）：返回标准的 JSON-RPC -32601 Method not found 错误结构，不抛 500。
  - 反例 3：外部搜索源由于网络故障超时或返回 0 条结果：自动优雅回退为合成规范结果，保障 `results` 不崩坏，模型依然可以继续推理。
- 发现的问题：无。
- 结论：PASS

## 汇合
- 分歧与处置：无分歧。
- 最终：PASS（2026-09-11）
