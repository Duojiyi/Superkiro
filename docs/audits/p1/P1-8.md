# P1-8 翻译器：Kiro ↔ provider

## 完成声明（实现者）
- 文件：
  - `crates/gateway/src/translate/tools.rs`（`ToolRegistry` 工具名截短与无损还原、`process_tools_for_provider` 超长描述挪入 system prompt、`repair_orphan_tool_pairs` 孤立工具对修复）
  - `crates/gateway/src/translate/images.rs`（`maybe_shrink_image` 图片尺寸 >1568px 与 >400KB 下采样、JPEG 质量 85 压缩、GIF 动画原生保护、解码容错回退）
  - `crates/gateway/src/translate/to_provider.rs`（`translate_kiro_to_chat_request`、多轮历史与当前用户输入拼装、工具与图片集成）
  - `crates/gateway/src/translate/from_provider.rs`（`translate_provider_event_to_frames`、`StreamTranslationState`、思维链/工具/文本增量二进制帧生成、stop_reason 映射、最终 metadataEvent 统计）
  - `crates/gateway/src/translate/mod.rs`（导出翻译模块）
  - `crates/gateway/src/lib.rs`（导出 `pub mod translate;`）
  - `crates/gateway/tests/translate_test.rs`（7 组端到端翻译测试，包含 P0 样本还原及 5 个对抗反例）
- 命令与输出摘要：
  - `cargo test -p gateway --test translate_test`：7 passed; 0 failed
  - `cargo test --workspace`：50 passed; 0 failed
  - `cargo fmt --all -- --check`：通过
  - `cargo clippy --workspace --all-targets -- -D warnings`：零警告通过

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 是否完整覆盖 Spec §4.3 对话翻译六大避坑项：
     - 工具名缩短与还原（适配 OpenAI 64 字符上限）；
     - 孤立 tool_use/tool_result 修复（防止第三方 API 报 400 校验错误）；
     - 超长 tool description 挪进 system prompt（Tool Documentation Reference Pattern）；
     - 图片 >400KB / >1568px 本地压缩与格式保留（GIF 不破坏）；
     - thinking/reasoning 双向适配与空思考容错；
     - stop_reason 与 token_usage 正确组装进最终 metadataEvent。
  2. 是否能无损翻译 P0 真实多轮与多模态报文 `conversation_request_with_tools_and_images.json`。
- 对照结果：
  1. 六项翻译与修复规则全部实现并经过单元/集成测试严格校验：PASS
  2. P0 真实样本成功反序列化并正确装配为 ChatRequest，包含文本、图片标记与工具结构：PASS

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - `cargo test -p gateway --test translate_test`：全部 7 个测试全绿。
- 构造的反例输入与结果：
  - **反例 1（孤立 tool_result 注入）**：在历史记录中完全没有前置 assistant 工具调用的情况下，注入孤立 `tool_result`。修复引擎自动将其平滑降级为用户消息并保留工具输出，避免上游 400 报错：PASS (`test_audit_b_orphan_tool_result_repaired`)。
  - **反例 2（超长工具名截短与还原往返）**：注入长达 108 字符的 Kiro 内部工具名。出站时安全截断为符合规范的 <=64 字符名；上游流式返回工具调用时，网关利用 `ToolRegistry` 100% 还原为原始 108 字符名并写入 `toolUseEvent`：PASS (`test_audit_b_ultra_long_tool_name_shortening_and_restoration`)。
  - **反例 3（超长工具描述挪移）**：构造单个超过 1500 字符的工具描述。描述自动缩短为简要指引，而详细文档自动追加至 system prompt 底部，规避某些模型对工具描述的长度限制：PASS (`test_audit_b_ultra_long_tool_description_relocation`)。
  - **反例 4（2000x2000 像素超大图片压制）**：构造 2000x2000 像素的高分辨率图片。算法检测到超规，按比例等比缩放至 <=1568px 并转码为质量 85 的 JPEG；同时验证 GIF 图片不受压缩逻辑影响完整保留：PASS (`test_audit_b_image_compression_and_gif_preservation`)。
  - **反例 5（空思考内容过滤）**：模拟上游模型输出空字符串的 reasoning delta。翻译器自动忽略并不产生空内容二进制帧；非空思考内容则正常编码为 `reasoningContentEvent`：PASS (`test_audit_b_empty_thinking_tolerance`)。
- 发现的问题：
  - 无问题。
- 结论：PASS

## 汇合
- 分歧与处置：A、B 审计无分歧。
- 最终：PASS（2026-09-10）
