# P1-7 provider 适配器 trait + OpenAI/Anthropic 实现

## 完成声明（实现者）
- 文件：
  - `crates/gateway/src/provider/mod.rs`（`ModelProvider` trait、`ProviderConfig`、`ChatRequest`/`ChatMessage`、`ProviderStreamEvent`/`ProviderDelta`/`TokenUsage`、`process_byte_stream` 流式帧行解析器）
  - `crates/gateway/src/provider/openai.rs`（`OpenAiProvider`：支持 text、reasoning、tool_calls 分片、usage 提取、`[DONE]` 终止）
  - `crates/gateway/src/provider/anthropic.rs`（`AnthropicProvider`：支持 message_start input tokens、thinking_delta、tool_use 块与 input_json_delta、message_delta output tokens、message_stop 终止）
  - `crates/gateway/src/lib.rs`（导出 `pub mod provider;`）
  - `crates/gateway/tests/provider_test.rs`（6 组 wiremock 上游模拟测试，覆盖两大 Provider Happy Path 与四种异常分支）
- 命令与输出摘要：
  - `cargo test -p gateway --test provider_test`：6 passed; 0 failed
  - `cargo test --workspace`：43 passed; 0 failed
  - `cargo fmt --all -- --check`：通过
  - `cargo clippy --workspace --all-targets -- -D warnings`：零警告通过

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 是否满足 Spec §15.2 可插拔上游适配器要求：定义统一的 `ModelProvider` trait，抽象出 `translate_request`, `parse_stream_line`, `extract_usage`, `chat_stream`。
  2. 是否提供完整的 OpenAI 与 Anthropic 兼容实现，支持流式 SSE 数据解析。
  3. 是否支持思维链/思考过程（`Reasoning` delta）、工具调用入参分片（`ToolCallChunk` delta）、文本输出（`Text` delta）。
  4. 是否提取完整 token 统计（包含提示词 tokens、补全 tokens 及缓存读取 tokens 如 `cache_read_input_tokens`）。
- 对照结果：
  1. Trait 包含全部四项方法，各 provider 仅关注自身协议序列化与 SSE 事件拆分：PASS
  2. `OpenAiProvider` 与 `AnthropicProvider` 均实现完整端到端 SSE 转换：PASS
  3. 支持 DeepSeek/o1 reasoning、Anthropic thinking、OpenAI `function.arguments` 及 Anthropic `input_json_delta`：PASS
  4. `TokenUsage` 统一归一化 prompt, completion, total 及缓存读写 tokens：PASS

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - `cargo test -p gateway --test provider_test`：全部 6 个测试全绿。
- 构造的反例输入与结果：
  - **反例 1（上游断流 - Premature Disconnect）**：上游发送部分数据后在未发送 `[DONE]` 或 `message_stop` 的情况下非正常中断关闭连接。适配器检测到异常截断，流式返回 `Err(ProviderError::StreamDisconnected)`，阻止网关误判正常结束：PASS (`test_audit_b_upstream_stream_disconnect`)。
  - **反例 2（上游超时 - Timeout）**：模拟上游响应延迟超过设定的 `timeout` 阈值。客户端正确触发并拦截返回 `Err(ProviderError::Timeout)`：PASS (`test_audit_b_upstream_timeout`)。
  - **反例 3（上游非 200 - HTTP Error）**：模拟上游返回 429 速率限制及错误 JSON。适配器捕获并返回 `Err(ProviderError::Http(StatusCode::TOO_MANY_REQUESTS, ...))`：PASS (`test_audit_b_upstream_non_200_error`)。
  - **反例 4（上游无 usage 上报）**：模拟上游流式返回纯文本并在最后 `[DONE]`，但没有任何 usage 块。适配器平稳完成流式输出，不发生反序列化崩溃或 panic：PASS (`test_audit_b_upstream_no_usage_reported`)。
- 发现的问题：
  - 无问题。
- 结论：PASS

## 汇合
- 分歧与处置：A、B 审计无分歧。
- 最终：PASS（2026-09-10）
