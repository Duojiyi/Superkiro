# P1-6 gateway：鉴权中间件（单卡密硬编码 + token_version）

## 完成声明（实现者）
- 文件：
  - `crates/gateway/src/auth.rs`（JWT 签发与校验、`AuthClaims`、`AuthState`、`auth_middleware`、`token_version` 即时吊销比对、AWS 错误映射）
  - `crates/gateway/src/lib.rs`（导出 `pub mod auth;`）
  - `crates/gateway/tests/auth_test.rs`（7 组集成测试，覆盖合法鉴权、无 Header、格式错误、篡改签名、即时吊销、过期 Token、冻结卡密）
- 命令与输出摘要：
  - `cargo test -p gateway --test auth_test`：7 passed; 0 failed
  - `cargo test --workspace`：37 passed; 0 failed
  - `cargo fmt --all -- --check`：通过
  - `cargo clippy --workspace --all-targets -- -D warnings`：零警告通过

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 是否支持 Bearer JWT 校验，并解析 `card_id`, `group_id`, `token_version`, `exp`, `iat`（Spec §4.1）。
  2. 是否实现基于 `token_version` 的即时吊销机制：比对 JWT 携带的 `token_version` 与服务端当前持有的版本号，不一致即刻拦截，无需等待 1 小时 TTL 自然过期。
  3. 拦截时是否返回标准 AWS 结构化 JSON 错误响应（HTTP 401 Unauthorized，包含 `__type` 与 `message`），而非 500。
  4. 验证通过后，是否将 `AuthClaims` 注入请求上下文供后续处理器读取用户身份。
- 对照结果：
  1. 完整解析全部 claims 字段，签名算法为 HMAC-SHA256：PASS
  2. `revoke_card` 自增版本号后，旧版本 token 在下一次请求时被即时拦截：PASS
  3. 映射到 `UnrecognizedClientException`, `ExpiredTokenException`, `TokenRevokedException`, `MissingAuthenticationTokenException`, `AccessDeniedException`：PASS
  4. 通过 `req.extensions_mut().insert(claims)` 成功传递并在 `protected_endpoint` 中解构读取：PASS

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - `cargo test -p gateway --test auth_test`：全部 7 个测试全绿。
- 构造的反例输入与结果：
  - **反例 1（签名篡改攻击）**：使用第三方非授权 Secret 签发格式合法的 Token，网关拦截并返回 401 `UnrecognizedClientException`：PASS (`test_audit_b_tampered_signature_rejected`)。
  - **反例 2（吊销即时性检验）**：签发合法 v1 Token，先通过一次请求；随后调用 `revoke_card` 使卡密升级至 v2；原 v1 Token 再次发起请求，被立即阻断并返回 401 `TokenRevokedException`，实证不需要等 1h TTL：PASS (`test_audit_b_revoked_token_version_mismatch_rejected`)。
  - **反例 3（过期 Token 注入）**：构造过去时间戳的 Token，网关返回 401 `ExpiredTokenException`：PASS (`test_audit_b_expired_token_rejected`)。
  - **反例 4（非法认证标头）**：分别测试缺失 `Authorization` 标头及 `Basic` 认证标头，均被精确拦截并返回 401：PASS (`test_auth_missing_header_rejected`, `test_auth_malformed_header_rejected`)。
  - **反例 5（冻结卡密阻断）**：将卡密标记为 `is_active: false`，持有有效 Token 的请求仍被拦截并返回 401 `AccessDeniedException`：PASS (`test_frozen_card_rejected`)。
- 发现的问题：
  - 无问题。
- 结论：PASS

## 汇合
- 分歧与处置：A、B 审计无分歧。
- 最终：PASS（2026-09-10）
