# P1-2 kiro-wire：帧解码（移植）

## 完成声明（实现者）
- 文件：
  - `crates/kiro-wire/src/error.rs`：AWS Event Stream 解析错误定义 (`ParseError`, `ParseResult`)
  - `crates/kiro-wire/src/crc.rs`：零依赖编译期常量表驱动 ISO-HDLC / IEEE 802.3 CRC32 算法
  - `crates/kiro-wire/src/header.rs`：支持 10 种头部类型 (`:event-type`, `:content-type`, `:message-type`, `:exception-type`) 解析
  - `crates/kiro-wire/src/frame.rs`：无状态单帧解析器（严格校验 12 字节 Prelude, Prelude CRC32, Message CRC32, 边界控制）
  - `crates/kiro-wire/src/decoder.rs`：四态流式事件状态机 (`Ready`, `Parsing`, `Recovering`, `Stopped`)
  - `crates/kiro-wire/src/lib.rs`：模块导出
  - `crates/kiro-wire/tests/decoder_test.rs`：集成测试套件，全面加载 `docs/p0/stream/*.bin` 6 个语料文件及对抗用例
- 命令与输出摘要：
  - `cargo test -p kiro-wire`：13 个测试全部 PASS (2 单元测试 + 11 个集成测试，0 失败)
  - `cargo fmt --all -- --check`：0 差异
  - `cargo clippy --workspace --all-targets -- -D warnings`：0 警告

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 是否完整实现 Spec §4.4 AWS 二进制 event-stream 协议（12B prelude, headers, payload, 4B message CRC32）。
  2. 是否成功解码 P0-6 捕获的所有 6 组二进制测试语料（纯文本、分片工具、思考链、上下文/元数据、空保活、异常帧）。
  3. 四态状态机与容错恢复机制是否完备。
- 对照结果：
  1. 帧格式与 CRC 校验完全遵循 AWS 规范：PASS
  2. P0-6 六组测试语料全部解码无误且字段精确对应：PASS
  3. 四态流式解码器实现完备：PASS

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - 运行 `cargo test -p kiro-wire --test decoder_test`，11 个测试全绿。
- 构造的反例输入与结果：
  - **反例 1（单字节极度切片）**：以 1 字节切片喂入整个文本流，验证状态机在任意碎片化输入下正确拼接完整帧：PASS (`test_streaming_byte_by_byte_feed`)。
  - **反例 2（Prelude CRC 破坏）**：篡改第 10 字节，解码器精准返回 `PreludeCrcMismatch` 并尝试向前移动 1 字节恢复：PASS (`test_corrupted_prelude_crc_recovery`)。
  - **反例 3（Message CRC 破坏）**：篡改末尾 CRC 字节，解码器精准返回 `MessageCrcMismatch`：PASS (`test_corrupted_message_crc_recovery`)。
  - **反例 4（非法超短帧）**：构造长度小于 16 字节的消息，解码器拒绝并返回 `MessageTooSmall`：PASS (`test_message_too_small_rejection`)。
  - **反例 5（连续严重损坏）**：连续灌入不可恢复乱码，解码器达到阈值后进入 `Stopped` 终止态并返回 `TooManyErrors`，绝不陷入死循环：PASS (`test_excessive_errors_stops_decoder`)。
- 发现的问题：
  - 无越界、无 panic。
- 结论：PASS

## 汇合
- 分歧与处置：A、B 审计无分歧。
- 最终：PASS（2026-09-10）
