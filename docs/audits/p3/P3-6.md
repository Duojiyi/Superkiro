# 双盲审计报告：P3-6 写登录态 + Kiro 进程重启

**日期**：2026-09-10  
**任务**：P3-6 写登录态 + Kiro 进程重启（`crates/patch-engine/src/process.rs`）（Spec §4.1, §9）  
**关联代码**：
- `crates/patch-engine/src/process.rs`
- `crates/patch-engine/src/lib.rs`
- `crates/patch-engine/tests/doctor_beacon_test.rs`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照 Spec §4.1（接管生命周期）、§9（客户端进程管理）规范核验：

| 规范条目 | 规范指标与技术要求 | 代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. Kiro 进程拉起注入 (Spec §9)** | 支持拉起 Kiro 进程并自动注入所有接管环境变量（`KIRO_AUTH_PORTAL_URL`, `KIRO_DISABLE_TELEMETRY`, `KIRO_DISABLE_AUTO_UPDATE`, `KIRO_GATEWAY_URL`）。 | `crates/patch-engine/src/process.rs::launch_kiro` | **PASS** |
| **2. 进程状态边界防护 (Spec §4.1)** | 当 Kiro 正在运行时，禁止直接拉起新实例引发数据冲突；提供停止与重启机制。 | `crates/patch-engine/src/process.rs::is_kiro_running`, `stop_kiro` | **PASS** |
| **3. 优雅终止与超时 (Spec §9)** | `stop_kiro` 在各操作系统（Windows `taskkill /IM kiro.exe`, macOS/Linux `killall`) 发送终止信号，并轮询等待退出，超时（默认 10s）返回明确错误。 | `crates/patch-engine/src/process.rs::stop_kiro` | **PASS** |
| **4. 一键重启助手 (Spec §4.1)** | `restart_kiro` 自动串联 `stop_kiro` 与 `launch_kiro`，并自动注入当前网关端点配置。 | `crates/patch-engine/src/process.rs::restart_kiro` | **PASS** |

**审计 A 结论**：Spec §4.1, §9 规范各项要求 100% 达成。**PASS**。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

针对进程不存在、重复停止、以及虚构可执行路径等异常场景进行对抗性测试：

### 对抗场景 1：可执行文件不存在拦截
- **验证**：调用 `launch_kiro` 指向不存在的路径。
- **实测结果**：立即返回 `ProcessError::ExecutableNotFound`，严禁启动空进程或 Panic。
- **实测结果**：**PASS**（见 `tests/doctor_beacon_test.rs::test_process_lifecycle_helpers`）。

### 对抗场景 2：幂等性终止
- **验证**：当 Kiro 进程原本就未运行时调用 `stop_kiro`。
- **实测结果**：直接返回 `Ok(())`，不报任何错误，确保上层可放心重试。
- **实测结果**：**PASS**（见 `tests/doctor_beacon_test.rs::test_process_lifecycle_helpers`）。

---

## 自动化测试与质量指标

- **单元与集成测试**：`cargo test -p patch-engine --test doctor_beacon_test test_process_lifecycle_helpers` -> **1 passed; 0 failed**
- **代码静态合规**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**

---

## 汇合结论

- **审计 A 结论**：PASS（进程环境变量注入、优雅停止与重启链路合规）  
- **审计 B 结论**：PASS（幂等停止与容错校验全部通过）  
- **最终审计结论**：**PASS**，任务 P3-6 正式完成。
