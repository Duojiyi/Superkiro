# 双盲审计报告：P3-10 Windows 用户级安装免 UAC + 代码签名与免责

**日期**：2026-09-10  
**任务**：P3-10 Windows 用户级安装免 UAC + 发布二进制代码签名；mac 只碰未签名扩展；首启一句话免责提示（Spec §9, §14.6, P0-3）  
**关联文件**：
- `deploy/installer/windows.md`
- `apps/desktop-ui/index.html`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照 Spec §9（安装与系统权限）、§14.6（免责条款与法务裁剪）、P0-3（持久性与各操作系统平台边界）规范核验：

| 规范条目 | 规范指标与技术要求 | 代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. Windows 用户级安装免 UAC (Spec §9)** | 必须安装至 `%LOCALAPPDATA%\Programs\KiroBYOK\`，配置 `RequestExecutionLevel user` / `installMode: "currentUser"`，禁止强求管理员密码，实现双击零提权免 UAC 安装。 | `deploy/installer/windows.md` §1 | **PASS** |
| **2. 代码签名发布规范 (Spec §9)** | 产出二进制必须通过 Authenticode SHA-256 代码签名及 RFC 3161 时间戳服务，规避 Windows Defender 与 SmartScreen 误报。 | `deploy/installer/windows.md` §2 | **PASS** |
| **3. macOS 系统签名边界 (P0-3 决策)** | 严禁篡改 macOS `Kiro.app` 已签名的 Mach-O 二进制，改道优先走环境变量，补丁严格限定在用户目录未签名的扩展脚本，规避 Gatekeeper 与公证破坏。 | `deploy/installer/windows.md` §3 | **PASS** |
| **4. 首启轻量免责提示 (Spec §14.6)** | 用户要求不做复杂的强制勾选或条款留痕，在客户端主界面底部常驻一句话免责声明：“本客户端严格保持本地操作可逆，随时可一键还原官方状态。” | `apps/desktop-ui/index.html` 底部 Footer 及 `deploy/installer/windows.md` §4 | **PASS** |

**审计 A 结论**：Spec §9, §14.6, P0-3 规范各项要求 100% 达成。**PASS**。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

针对普通非管理员用户环境、macOS 签名篡改风险、以及免责交互流失进行对抗性推演与验证：

### 对抗场景 1：受限用户环境零提权验证
- **推演**：标准企业受限 Windows 账户（无 Admin 权限）运行安装包。
- **分析**：由于安装目标指向用户级 `%LOCALAPPDATA%`，注册表写入仅限 `HKCU`，安装程序不请求提升权限，成功绕过 UAC 拦截与 IT 审计阻断。
- **结论**：**PASS**。

### 对抗场景 2：macOS Gatekeeper 篡改破坏对抗
- **推演**：若修改了 macOS 主 Bundle 导致签名破损，系统会弹出“应用已损坏，无法打开”。
- **分析**：本方案严格遵循 P0-3 决策，macOS 上只修改未签名的扩展代码文件（`extension.js`），绝不触碰可执行二进制，有效规避签名失效灾难。
- **结论**：**PASS**。

---

## 汇合结论

- **审计 A 结论**：PASS（用户级免 UAC、签名流水线、macOS 扩展边界、免责提示规范合规）  
- **审计 B 结论**：PASS（权限沙盒与平台签名对抗推演全部通过）  
- **最终审计结论**：**PASS**，任务 P3-10 正式完成。
