# 双盲审计报告：P3-5 settings.json 安全合并

**日期**：2026-09-10  
**任务**：P3-5 settings.json 安全合并（只碰我们管理的键，update.mode=none）（Spec §2.5, §9, §14.5, P0-3）  
**关联代码**：
- `crates/patch-engine/src/settings.rs`
- `crates/patch-engine/src/lib.rs`
- `crates/patch-engine/tests/takeover_test.rs`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照 Spec §2.5（辅助面成本管控）、§9（客户端配置合并）与 §14.5（精确回滚）规范核验：

| 规范条目 | 规范指标与技术要求 | 代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. 最小管理键范围 (Spec §14.5)** | 严格限定在 `MANAGED_KEYS` 范围内（`kiroAuthConfig`, `codewhisperer.config`, `kiroAgent.enableTabAutocomplete`, `update.mode`, `telemetry.telemetryLevel`），严禁篡改或擦除用户的字体、主题、按键映射或第三方插件配置。 | `crates/patch-engine/src/settings.rs::MANAGED_KEYS`, `merge_byok` | **PASS** |
| **2. 额度保护关闭自动补全 (Spec §2.5)** | 注入 `"kiroAgent.enableTabAutocomplete": false`，防止后台高频触发 `GenerateCompletions` 消耗第三方模型积分。 | `crates/patch-engine/src/settings.rs::merge_byok` | **PASS** |
| **3. 静默升级锁定 (Spec §9, P0-3)** | 注入 `"update.mode": "none"`，防止 Kiro 在后台自动静默拉取官方新版本覆盖补丁导致断服。 | `crates/patch-engine/src/settings.rs::merge_byok` | **PASS** |
| **4. 跨平台路径解析 (P0-3 规范)** | Windows (`%APPDATA%\Kiro\User\settings.json`)、macOS (`~/Library/Application Support/Kiro/User/settings.json`)、Linux (`~/.config/Kiro/User/settings.json`) 自动正确解析。 | `crates/patch-engine/src/settings.rs::default_settings_path` | **PASS** |
| **5. 原子落盘写入 (P0-4 健壮性)** | 写入过程使用同目录临时文件刷盘同步后重命名，防止 VS Code / Kiro 监听读到破损的 JSON。 | `crates/patch-engine/src/settings.rs::atomic_write` | **PASS** |

**审计 A 结论**：Spec §2.5, §9, §14.5 及 P0-3 规范各项要求 100% 达成。**PASS**。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

针对已有丰富自定义配置的文件、空白文件、不存在的文件与恢复一致性实施对抗性测试：

### 对抗场景 1：复杂用户配置保留实测
- **验证**：给 `SettingsManager` 喂入包含自定义字号、颜色主题、第三方插件自定义配置及用户已有的 `update.mode="manual"`。
- **实测结果**：
  - 合并后：用户的 `editor.fontSize`、`workbench.colorTheme`、`customExtension.key` 原样保留，仅精准追加了网关路由与更新拦截；
  - 恢复后：`update.mode` 精确复原为用户的 `"manual"`，所有 BYOK 键被干净卸载。
- **实测结果**：**PASS**（见 `tests/takeover_test.rs::test_settings_safe_merge_and_revert`）。

### 对抗场景 2：原本不存在 `settings.json` 的全新安装还原
- **验证**：针对尚未生成配置文件的系统执行合并与回滚。
- **实测结果**：合并时自动创建目录并生成标准 JSON；回滚时安全删除新建的配置文件，不遗留任何空壳文件。
- **实测结果**：**PASS**（见 `tests/takeover_test.rs::test_settings_empty_file_creation_and_revert`）。

---

## 自动化测试与质量指标

- **单元与集成测试**：`cargo test -p patch-engine --test takeover_test` -> **5 passed; 0 failed**
- **代码静态合规**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**

---

## 汇合结论

- **审计 A 结论**：PASS（安全合并、用户键保护、自动更新锁定与路径解析完全合规）  
- **审计 B 结论**：PASS（复杂配置保留与全新文件回滚对抗验证全量通过）  
- **最终审计结论**：**PASS**，任务 P3-5 正式完成。
