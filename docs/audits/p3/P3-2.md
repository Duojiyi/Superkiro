# 双盲审计报告：P3-2 patch-engine：Kiro 检测 + 版本识别 + 运行态检测 + 单实例锁

**日期**：2026-09-10  
**任务**：P3-2 patch-engine：Kiro 检测（Win/mac/Linux）+ 版本识别 + 运行态检测 + 单实例锁（Spec §9, P0-2, P0-7）  
**关联代码**：
- `crates/patch-engine/src/detect.rs`
- `crates/patch-engine/src/runtime.rs`
- `crates/patch-engine/src/lib.rs`
- `crates/patch-engine/tests/detect_runtime_test.rs`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照系统架构规范 Spec §9（客户端与接管引擎）、P0-2（环境变量与覆盖面）及 P0-7（辅助面关闭与运行态检测）核验四项核心能力：

| 规范条目 | 规范指标与技术要求 | 代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. 跨平台安装目录发现 (Spec §9)** | 扫描三平台候选路径：Windows (`%LOCALAPPDATA%\Programs\Kiro`, `Program Files`)、macOS (`/Applications/Kiro.app`)、Linux (`/opt/kiro`, `/usr/share/kiro`, `~/.local/share/kiro`)，支持用户自定义绿色版路径。 | `crates/patch-engine/src/detect.rs::get_candidate_install_paths`, `detect_kiro` | **PASS** |
| **2. 精准版本与元数据提取 (Spec §9, P0-2)** | 解析 `product.json` 与 `package.json`，提取 Kiro 版本（如 `1.0.437`）、VSCode 版本（`1.109.5`）、commit hash、quality、`win32MutexName`（`"kiro"`），并检测 `kiro.kiro-agent` 扩展版本（`1.0.794`）。 | `crates/patch-engine/src/detect.rs::inspect_installation_dir` | **PASS** |
| **3. 运行态进程与文件安全边界 (Spec §9, P0-7)** | 接管/恢复/写 token/打补丁前必须检测 Kiro 是否在运行，运行中禁止改动文件避免半状态。跨平台检测目标二进制运行态 (`is_kiro_running`)。 | `crates/patch-engine/src/runtime.rs::is_kiro_running`, `is_process_running_by_name` | **PASS** |
| **4. 客户端单实例互斥锁 (Spec §9)** | 客户端自身必须具备单实例锁（防两份客户端启动器争抢补丁与环境变量修改）；崩溃遗留的僵尸锁文件必须支持 PID 存活检测并自动安全接管。 | `crates/patch-engine/src/runtime.rs::SingleInstanceLock` | **PASS** |

**审计 A 结论**：Spec §9 与 P0 阶段制定的版本识别与运行态约束 100% 达成。**PASS**。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

针对真机环境、沙箱坏损数据、并发互斥冲突与崩溃恢复实施对抗性测试：

### 对抗场景 1：真机真实环境 Kiro 1.0.437 检测
- **验证**：在当前真实 Windows 宿主环境下调用 `detect_kiro(None)`。
- **实测结果**：
  - 精确命中安装路径：`C:\Users\Administrator\AppData\Local\Programs\Kiro`；
  - 成功提取 `version: "1.0.437"`, `vscode_version: "1.109.5"`, `win32_mutex_name: "kiro"`, `agent_version: "1.0.794"`, `is_user_level: true`；
  - 与 P0-2 真实逆向调查结论完全一致。
- **实测结果**：**PASS**（见 `tests/detect_runtime_test.rs::test_detect_real_host_kiro_installation`）。

### 对抗场景 2：空目录与虚假文件防御
- **验证**：给检测引擎喂入空目录或缺损 `product.json`/`package.json` 的伪装目录。
- **实测结果**：检测函数安全拦截并返回标准错误 `DetectError::InvalidInstallation`，杜绝误报或向无效路径注入文件。
- **实测结果**：**PASS**（见 `tests/detect_runtime_test.rs::test_invalid_installation_detection`）。

### 对抗场景 3：客户端单实例锁并发抢占与僵尸锁恢复
- **验证**：
  1. 实例 A 获取单实例锁；
  2. 模拟先前的客户端异常崩溃（写入一个不存在的死循环 PID 如 99999999）；
  3. 新实例 B 启动尝试获取单实例锁。
- **实测结果**：
  - `is_pid_alive` 准确识别该 PID 已消亡，新实例立即执行安全自愈并接管锁文件；
  - 当进程正常退出或 RAII Guard `drop` 时，锁文件被 100% 物理清除，无残留垃圾。
- **实测结果**：**PASS**（见 `tests/detect_runtime_test.rs::test_single_instance_lock_contention_and_stale_recovery`）。

---

## 自动化测试与质量指标

- **单元与集成测试**：`cargo test -p patch-engine` -> **21 passed; 0 failed**
- **代码静态合规**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**
- **原生标准实现**：零引入第三方进程分析或重量级库，纯原生指令与系统工具实现，耗时低、无运行期开销。

---

## 汇合结论

- **审计 A 结论**：PASS（三平台目录匹配、版本字段、运行态探测与单实例互斥均达标）  
- **审计 B 结论**：PASS（真机 1.0.437 检测通过、坏损目录容错及僵尸锁自愈完全健壮）  
- **最终审计结论**：**PASS**，任务 P3-2 正式完成。
