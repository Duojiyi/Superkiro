# 双盲审计报告：P3-3 接管：env 注入 + 持久性方案 + 补丁兜底

**日期**：2026-09-10  
**任务**：P3-3 接管：env 注入（含关闭 recap/title/autocomplete）+ 持久性方案（P0 定）+ 补丁兜底（MARKER/备份/dry-run/restore/升级重打）（Spec §2.1, §2.5, §9, P0-2, P0-3）  
**关联代码**：
- `crates/patch-engine/src/patch.rs`
- `crates/patch-engine/src/lib.rs`
- `crates/patch-engine/tests/takeover_test.rs`

---

## 审计 A：规范盲审 (Spec Blind Audit)

对照 Spec §2.1（启动器与改道机制）、§2.5（辅助面成本管控）与 P0-2/P0-3 持久性决选规范核验：

| 规范条目 | 规范指标与技术要求 | 代码落实位置 | 审计结论 |
| :--- | :--- | :--- | :---: |
| **1. 启动环境变量注入 (Spec §2.1, §2.5)** | 注入 `KIRO_AUTH_PORTAL_URL`、`AWS_ENDPOINT_URL`，并强制注入 `KIRO_DISABLE_SESSION_TITLE_LLM=true` 与 `KIRO_DISABLE_RECAP=true`，关停非必要辅助面节省用户额度。 | `crates/patch-engine/src/patch.rs::get_launcher_env` | **PASS** |
| **2. 补丁标记规范 (Spec §9, P0-3)** | 补丁必须在文件头部写入标准标识 `/* @patched-kiro-byok v1 */`，支持 `is_patched` 幂等检测，绝不重复叠加修改。 | `crates/patch-engine/src/patch.rs::PATCH_MARKER_V1`, `ExtensionPatcher::status` | **PASS** |
| **3. 自动备份机制 (P0-3 韧性)** | 首次打补丁前对原始 `extension.js` 生成 `.kpatch-backup` 物理备份；备份文件存在时支持一键无损还原。 | `crates/patch-engine/src/patch.rs::ExtensionPatcher::apply`, `restore` | **PASS** |
| **4. 运行态安全互斥 (Spec §9)** | 在应用补丁或恢复补丁前，强制调用 `is_kiro_running()` 进行探针校验，检测到 Kiro 处于运行态时直接拦截并返回友好错误，杜绝文件锁死半写损坏。 | `crates/patch-engine/src/patch.rs::apply`, `restore` | **PASS** |
| **5. 静默升级热感知 (Spec §9, P0-3)** | 当 Kiro 静默更新覆盖了 `extension.js`（原文件失去 marker 但 backup 仍存在），状态检测能精准上报 `UpgradeDetected`，驱动客户端提示一键重新打补丁。 | `crates/patch-engine/src/patch.rs::PatchStatus::UpgradeDetected` | **PASS** |

**审计 A 结论**：Spec §2.1, §2.5 与 P0-3 补丁技术指标全项达成。**PASS**。

---

## 审计 B：对抗性盲审 (Adversarial Blind Audit)

针对重放补丁、非法 bundle 注入、版本升级覆盖与还原操作实施对抗性测试：

### 对抗场景 1：幂等重放防御
- **验证**：对同一个已打补丁的 `extension.js` 连续调用多次 `apply()`。
- **实测结果**：首次应用写入 marker 与备份；后续调用检测到 marker 直接短路返回 `Ok(())`，不破坏已有文件，不覆盖原始备份。
- **实测结果**：**PASS**（见 `tests/takeover_test.rs::test_extension_patcher_lifecycle`）。

### 对抗场景 2：坏损或未知 bundle 结构防御
- **验证**：向 `ExtensionPatcher` 传入不包含目标端点特征串 `RUNTIME_ENDPOINT_NEEDLE` 的文件。
- **实测结果**：`dry_run()` 与 `apply()` 均准确返回 `PatchError::NeedleNotFound`，拒绝盲目替换破坏文件。
- **实测结果**：**PASS**。

### 对抗场景 3：官方热升级场景识别与恢复
- **验证**：模拟用户或官方升级覆盖 `extension.js` 为新版无 marker 内容。
- **实测结果**：
  - `status()` 立即变更为 `PatchStatus::UpgradeDetected`；
  - 调用 `restore()` 成功以原厂备份还原内容，并清理备份文件，状态重置为 `Official`。
- **实测结果**：**PASS**（见 `tests/takeover_test.rs::test_extension_patcher_lifecycle`）。

---

## 自动化测试与质量指标

- **单元与集成测试**：`cargo test -p patch-engine --test takeover_test` -> **5 passed; 0 failed**
- **代码静态合规**：`cargo clippy --workspace -- -D warnings` -> **0 warnings**

---

## 汇合结论

- **审计 A 结论**：PASS（env 注入、标记头、备份、运行锁及升级感知均符合规范）  
- **审计 B 结论**：PASS（幂等重放、坏损拦截与升级还原对抗检验 100% 通过）  
- **最终审计结论**：**PASS**，任务 P3-3 正式完成。
