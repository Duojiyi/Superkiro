# 连接失败与余额消失排查

## 已确认并修复

- 卡密验证未启用连接时，切到用量页调用了需要桌面会话的接口，错误状态又隐藏了概览余额。
- 旧版线上用量响应缺少 availableCredits 时，客户端可能覆盖已有余额。现在保留最近确认值并标注时间、非实时；不通过总额减用量伪造余额。
- 诊断报告第 6、7 项白名单缺少现有检查名称，现显示实际脱敏名称。
- 宿主未持久保存连接操作的失败阶段。现在只保存最近已结束操作的白名单阶段、错误码、HTTP 状态、完成时间；重启可读取。不保存远程响应、卡密、令牌、地址或本地路径。成功操作会替换旧记录，不是完整历史日志。

## 本次连接失败尚未定位

用户报告时间 2026-09-19 20:09:39。检查时 Superkiro 与 Kiro 均未运行，没有可读取的已记住卡密。旧版本没有持久失败记录，因此无法恢复该次原始错误。

本地没有桌面 session JSON；零字节 session-lock 是正常锁文件，不等于锁故障。实际安装的只读预检通过，包括补丁/设置验证及启动命令准备，不代表 OAuth 授权、配置应用或真实 IDE 对话通过。未重新绑定卡密、修改 IDE 配置、关闭 TLS 验证、推送或部署。

## 验证

- desktop-host：15 项通过；额外显式执行实际安装只读预检，1 项通过。
- desktop-host all-targets clippy -D warnings 通过。
- UI 回归及 Debug 编译结果见本次交付说明。

下一次使用更新后的 Debug 发起连接，若失败，诊断报告会显示阶段及脱敏代码，可据此继续定位。不得将本补丁描述为已经修复该次未知连接失败。

## 后续定位：卡密绑定规则

新版持久诊断确认后续连接在 authenticate 阶段被服务端以 HTTP 400 拒绝（auth-rejected）。服务端同时限制“一卡一设备”和“一设备一卡”，后者不符合产品要求，会拒绝同一电脑换用另一张卡。已删除 activate_card_with_device 与 bind_device 两处反向唯一性限制，保留一卡一设备、换绑规则与令牌撤销机制。不能凭此推断用户此前每次失败均同源。

billing card_ops 9 项、tier_policy 6 项、gateway auth_handshake 6 项通过。回归覆盖同机新卡成功、新卡另一设备拒绝以及令牌卡密身份。生产上线由限定候选流程单独记录。

## 生产部署完成

2026-09-19 21:37（Asia/Shanghai）上线限定版本 `20260919T130652Z`。从原生产源码创建候选，仅修改绑定 guard、认证回归测试和 Compose 镜像号，未发布其他本地未验收改动。服务器 6 项认证回归通过；线上临时卡 A/B 同机轮换及切回通过，两张卡分别拒绝另一设备 403。临时卡均已禁用并独立复查为 banned，现有用户卡未改动。备份完整，gateway healthy，部署锁释放。报告：`deploy/device-reuse-release-results.json`。主线程另行直连核对 /healthz 200、未授权管理员接口 401。

这验证了服务端卡密绑定修复，不等同于真实 IDE 消息、模型选择及计费全流程重新验收。
