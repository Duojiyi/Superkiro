# P0-4 抓真实登录态与回调

## 完成声明（实现者）
- 文件：
  - `docs/p0/auth.md`：详细分析报告；
  - `docs/p0/samples/auth_token_sample.json`：脱敏样本文件。
- 命令与输出摘要：
  - 读取并解析了本机 Kiro 1.0.437 真实生成的 `%USERPROFILE%\.aws\sso\cache\kiro-auth-token.json`；
  - 逆向了 `extension.js` 中 `TokenStorage` 与 `AuthSSOServer` 源码，全面解答了 Spec §16-3、§16-5。

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 准确指明 1.0.x 登录态落盘路径与 JSON schema 字段；
  2. 说明登录回调端口机制、回调路径（`/oauth/callback`）与参数（`code`, `state`）；
  3. 给出 Spec §16-3 与 §16-5 的明确结论；
  4. 确认样本已安全脱敏。
- 对照结果：
  - 标准 1：PASS（路径为 `~/.aws/sso/cache/kiro-auth-token.json`，schema 包含 6 个核心字段）
  - 标准 2：PASS（详细记录了 `AuthSSOServer` 本地随机端口、超时及重定向逻辑）
  - 标准 3：PASS（确认 Kiro 客户端不对 JWT 签名做任何校验，可自由签发）
  - 标准 4：PASS（样本中的真实 Token 与敏感哈希已全部使用 `SAMPLE_REDACTED_*` 替换）

## 审计 B（对抗盲审）
- 安全与脱敏检查：
  - 扫描生成的文档与样本文件，确认无真实 Bearer Token、Secret 或隐私 ARN 提交至代码库。检查通过。
- 鲁棒性与异常路径分析：
  - 审查：如果直接覆盖修改 `kiro-auth-token.json`，是否会导致正在运行的 Kiro 状态不一致？
  - 发现：Kiro `TokenStorage` 内置了文件监控 `watchFile`，当文件被外部更新时会清空内存缓存并通过 `_onDidChange` 派发事件，原生支持免重启热感知新 Token。
  - 审查：`KIRO_MACHINE_TOKEN` 是否会影响认证？证实仅为遥测 attribution 枚举值，不干扰认证链路。
- 结论：PASS

## 汇合
- 分歧与处置：无。
- 最终：PASS（2026-09-10）
