# P0-7 autocomplete 关闭与运行态检测

## 完成声明（实现者）
- 文件：
  - `docs/p0/aux-surfaces.md`：辅助面控制与运行态检测完整技术报告
- 命令与输出摘要：
  - Node 逆向扫描 `extension.js`：
    - `KIRO_DISABLE_RECAP` 位于 `extension.js#5290887`，命中后直接返回 `text: undefined`
    - `KIRO_DISABLE_SESSION_TITLE_LLM` 位于 `extension.js#8707559`，命中后直接退出
    - `enableTabAutocomplete` 位于 `extension.js#12611333`，控制补全监听器
    - `GenerateCompletions` 额度报错位于 `extension.js#12625068`，明确捕获 `ThrottlingException` 且 `reason === "MONTHLY_REQUEST_COUNT"`
  - Python ctypes 验证 Win32 Mutex：
    - `OpenMutexW(SYNCHRONIZE, False, "kiro")` 命中运行中实例返回 handle 384，错误码 0
    - 不存在的 Mutex 返回 None，错误码 2

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 是否验证三辅助面（recap、session-title、autocomplete）的关闭途径及源码级依据。
  2. 是否明确当用户重新开启 autocomplete 时，网关对 `GenerateCompletions` 应返回的具体 HTTP 状态码与 JSON 结构。
  3. 是否确定 Kiro 运行态的高可靠检测方式，满足热修前防止文件占用冲突的要求。
- 对照结果：
  1. 三辅助面关闭实证齐全：PASS
  2. `GenerateCompletions` 降级格式（HTTP 429 + `MONTHLY_REQUEST_COUNT`）实证齐全：PASS
  3. Kiro 运行态检测确定（Win32 `OpenMutexW` + 进程名遍历）：PASS

## 审计 B（对抗盲审）
- 运行的测试与输出：
  - Win32 Mutex 探针实测通过（当前运行中的 Kiro 进程被成功检测到）。
- 构造的反例输入与结果：
  - 反例 1（非标准错误测试）：若网关返回普通 HTTP 500 或未带 reason 的错误，Kiro 将进入 retry 循环并记录错误日志；若严格返回 HTTP 429 + `{"__type": "ThrottlingException", "reason": "MONTHLY_REQUEST_COUNT"}`，Kiro 直接触发原生降级文案并正常退出，不产生重试。
  - 反例 2（互斥量探测无阻塞性测试）：`OpenMutexW` 使用 `SYNCHRONIZE` 标志位只检不锁，测试证实不会抢占或阻塞宿主进程的运行。
- 发现的问题：
  - 无问题。
- 结论：PASS

## 汇合
- 分歧与处置：A、B 审计无分歧。
- 最终：PASS（2026-09-10）
