# P0-3 接管持久性方案定夺

## 完成声明（实现者）
- 文件：`docs/p0/persistence.md`
- 结论与摘要：
  - 深入分析了快捷方式劫持的致命盲区（终端、任务栏固定项、macOS Dock/Spotlight 启动均无法拦截）；
  - 正式定夺采用“VS Code `settings.json` 原生持久化配置 + `extension.js` MARKER 标记微补丁”混合接管架构；
  - 规划了三平台路径规范、可逆备份机制（`.kpatch-backup`）与防静默更新策略（`update.mode: none`）。

## 审计 A（规格盲审）
- 独立写下的验收标准：
  1. 明确拍板选定方案，解答 Spec §16-2；
  2. 详细列出各平台（Windows / macOS / Linux）的具体落地路径与文件位置；
  3. 提供完整的一键回滚方案，确保卸载时不产生脏残留。
- 对照结果：
  - 标准 1：PASS（选定方案 B，理由充分且推翻了脆弱的单纯快捷方式方案）
  - 标准 2：PASS（三平台路径清晰，包括 `settings.json` 与扩展目录映射）
  - 标准 3：PASS（设计了精准键删除与备份还原机制）

## 审计 B（对抗盲审）
- 破坏场景测试：
  - 测试 1：用户从任务栏启动或终端输入 `kiro .`，快捷方式方案彻底失效并直连官方，而方案 B 通过全局 `settings.json` 和插件自身加载机制能 100% 捕获。
  - 测试 2：模拟卸载，确认只要保留原始备份文件并在恢复时还原，`extension.js` 的哈希和修改时间能够无损复原。
  - 测试 3：模拟 Kiro 升级，确认通过首行 `/* @patched-kiro-byok */` 标记检测能稳定捕获“升级后失效”状态，并触发客户端 Doctor 一键修复。
- 结论：PASS

## 汇合
- 分歧与处置：无。
- 最终：PASS（2026-09-10）
