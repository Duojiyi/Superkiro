# 候选版本部署与测试准备

时间：2026-09-18 23:07（Asia/Shanghai）

## 发布结果

- 服务器：160.202.47.98。
- 版本/镜像：`20260918T144757Z` / `kiro-byok:20260918T144757Z`。
- 当前路径：`/opt/kiro-byok/current` 指向 `/opt/kiro-byok/releases/20260918T144757Z`。
- 切换前备份：`/opt/kiro-byok/backups/release-20260918T144757Z`，包含停写后的账本、部署文件与受保护配置。
- 旧镜像：`kiro-byok:20260918-search-fix`；未删除。
- 网关 healthy，Caddy running。上游、TLS 与其他受保护环境配置和发布前逐字节一致。

## 已通过的线上验证

- 网关 E2E：23 项通过。真实 Kimera 流式调用转换 AWS EventStream、工具调用、积分实际扣除、重复调用不重复扣费、鉴权、刷新令牌重放拦截、冻结/解冻均通过。
- 使用隔离临时分组验证原子配置发布、追加计划价格版本、旧 revision 拒绝；未修改客户分组或已有模型映射。
- 实际重启网关后余额、设备绑定、分组/价格配置和审计记录保留。
- 管理端 9 个页面浏览器检查通过，无捕获到的页面异常或服务端错误；配置编辑器与无发布原因时按钮禁用通过。
- 门户查询、激活、解绑确认/取消、无效充值码拒绝、移动端四页不溢出、帮助弹窗通过。未进行真实成功充值核销。
- 本机直连和本机代理均通过受信 CA 校验后的 HTTPS 访问；门户及管理端 HTML 与本地发布产物一致。浏览器测试固定服务器证书 SPKI，没有关闭全局 TLS 校验。
- 自动测试使用独立临时卡，结束后封禁，保留审计。隔离发布探针配置保留用于追溯。
- 客户验收卡只读核验：未激活、无设备绑定，200 积分；未用于自动调用或扣费。

## 人工验收入口

- 用户门户：https://160.202.47.98/portal
- 管理后台：https://160.202.47.98/admin/
- 本地：运行根目录 `启动Debug客户端.bat`，使用编译 Debug 和正常客户界面；默认网关已内置。
- 保存 Kiro 工作后输入原验收卡，通过客户端接管/启动入口测试。记录路径识别、证书安装、已运行 IDE 的关闭/重启，以及未运行时自动启动结果。
- 在真实 Kiro 中验证模型列表、不同模型聊天与工具请求、后台映射对应关系、门户积分变化和重新启动后的会话。
- IP HTTPS 使用私有 CA。出现证书提示时应核验并安装可信 CA，不应绕过证书错误。

## 尚未完成的范围

这次发布仅用于核心闭环人工验收，不是全功能上线审计通过证明。TOTP 2FA、数据库部署级 RLS 尚未实现；外部搜索与专用补全未完整生产验收。真实 Kiro IDE 完整交互尚未通过人工验收。普通 Kiro 快捷方式的注入环境、自包含分发包，以及所有失败阶段的凭据/云端绑定自动回滚仍未解决。配置发布有未结算请求时拒绝，属于空闲期发布。专用补全 E2E 仅验证显式限流响应，并不代表补全成功。

## 证据与回滚

本地结果文件：`deployment-candidate-results.json`、`deployment-e2e-results.json`、`deployment-browser-results.json`、`deployment-portal-v2-browser-results.json`、`deployment-candidate-verification.json`。日志位于 `.acceptance/server-release.log` 与 `.acceptance/candidate-*.log`。

发布脚本具备切换阶段自动回滚。人工测试或客户写入之后，不可直接用旧账本覆盖当前数据。后续回滚须先停写、保留现有账本和新增审计，再决定兼容迁移或恢复方案，防止丢失充值、扣费或绑定记录。
