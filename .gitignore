# Rust Build Artifacts
/target/
**/*.rs.bk
Cargo.lock.orig

# Node.js & Frontend Build Outputs
node_modules/
apps/admin-ui/node_modules/
apps/admin-ui/dist/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime State & Persisted Billing Data
/data/*.json
/data/*.json.*
/data/*.tmp
/data/billing_state.json*
/data/snapshots/

# Environment Variables & Secrets
.env
.env.*
!.env.example
*.pem
*.key
*.cert

# The patterns above miss the filenames this deployment actually uses:
# `.env.*` does not match `gateway.env`, and `*.cert` does not match `root.crt`.
*.env
*.crt
admin-access.json
id_rsa
id_rsa.*
id_ecdsa
id_ecdsa.*
id_ed25519
id_ed25519.*
known_hosts
known_hosts.*
# Release metadata naming server paths and image digests; written by the
# staging step and read by promotion. Never belongs in a commit.
/deployment-candidate-results.json

# Python & Temporary Debug Scripts Artifacts
__pycache__/
*.py[cod]
*$py.class
*.log
launch.log
*.tar.gz
*.zip

# Visual Regression Test Artifacts & Screen Captures
*.png
!crates/desktop-host/icons/icon.png
*.jpg
*.jpeg
*.svg.bak

# OS Metadata
.DS_Store
Thumbs.db
ehthumbs.db

# Isolated IDE acceptance profile and temporary credentials
/.acceptance/

# Local-only assets and packaging output
/references/
/.pi-glla/
/build/
/dist/
/*.spec
!deploy/server-ca.pem

target/


.build-check/

*.bak

# Tauri platform-generated schemas (regenerated by tauri-build)
/crates/desktop-host/gen/
